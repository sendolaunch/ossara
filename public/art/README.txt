PUT 2D ART IMAGES HERE (from ChatGPT or wherever).

The game looks for these by name. Drop a file in, refresh the browser.
If a file isn't here, the game falls back to its built-in styling — nothing breaks.

  logo.png            <- the plague-doctor mask logo (square, token/X avatar art)
  opening-bg.png      <- wide title background behind the logo (~1920x1080)
  opening-bg.mp4      <- OPTIONAL animated version of the title background (looping,
                         silent). If present it plays; opening-bg.png is the poster.
  classselect-bg.png  <- (optional) background behind the class-select screen
  hub-bg.png          <- (optional) background behind the Undercroft hub

PNG or JPG both fine (keep the same name, e.g. opening-bg.jpg also works if you
update the filename in src/ui/screens.js — just tell me and I'll switch it).
